import { StyleSheet } from 'react-native';

export const themeColor = {
  'background-grey': '#EAEAEA',
};

export const themeStyles = StyleSheet.create({
  'background-grey': {
    backgroundColor: themeColor['background-grey'],
  },
});
